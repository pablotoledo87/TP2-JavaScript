
for (let filas = 1; filas <= 30 ; filas++) {

    for (let columnas = 1; columnas < filas; columnas++) {
        document.write(`${filas}`)
    }
    document.write(`${filas}`);
    document.write("</p>");
}